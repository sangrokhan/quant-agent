# Scaffold-level dependencies (in addition to pyproject.toml's uv-managed
# deps for src/quant_agent). Kept here too so `pip install -r requirements.txt`
# works without uv for anyone just running the scaffold scripts directly.

pandas>=2.2
pyarrow>=17.0
yfinance>=0.2.40
ccxt>=4.3
vectorbt>=0.26
plotly<5.24  # pin: plotly>=5.24 removed scattermapbox, which vectorbt<=1.1.0's
             # theme init (_settings.py reset_theme) still references at
             # import time -- newer plotly breaks `import vectorbt` entirely.
             # Verified 2026-09-03: plotly 7.0.0 -> ImportError, plotly 5.23.0 -> OK.

# dev/test
pytest>=8.0
